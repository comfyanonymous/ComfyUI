# Optional Docker build mirrors. Copy to .env if Docker Hub or Debian mirrors are slow.
# PYTHON_BASE_IMAGE=public.ecr.aws/docker/library/python:3.10-slim-bookworm
# APT_MIRROR=http://mirrors.aliyun.com/debian
# APT_SECURITY_MIRROR=http://mirrors.aliyun.com/debian-security

# Optional runtime proxy for model and custom-node downloads.
# HTTP_PROXY=http://host.docker.internal:7897
# HTTPS_PROXY=http://host.docker.internal:7897
# NO_PROXY=localhost,127.0.0.1,::1
