Fork policy for this repository:

- Upstream GitHub Actions workflows have been removed from this fork.
- This fork is used for local workflow experimentation, especially soccer highlight tooling.
- CI and release automation from upstream are intentionally not carried over because they are unnecessary for this personal fork and can trigger unwanted runs on push or merge.

If GitHub Actions are needed later, add back only the minimum workflows required for this fork.
